<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
  <title>Task List</title>
  <script type="module" src="https://cdn.skypack.dev/@hotwired/turbo"></script>
</head>
<body>
  <header>
    <h1>Task List</h1>
  </header>
  <main>
    <div id="form-container">
      <form action="/tasks" method="post" data-turbo="true">
        <div>
          <input type="text" name="task_name" placeholder="Add a task" required />
          <input type="date" name="due_date" />
          <button type="submit">Submit</button>
        </div>
      </form>
    </div>

    <h2>Your Tasks</h2>
    <ul id="list">
      <% if @tasks["list"].any? %>
        <% @tasks["list"].each_with_index do |task, index| %>
          <li>
            <span><strong><%= task[:name] %></strong></span> - 
            <span>Due: <%= task[:due_date] %></span> - 
            <span>Status: <%= task[:complete] ? 'Complete' : 'Incomplete' %></span>
            <form action="/tasks/edit/<%= index %>" method="get" data-turbo="true" style="display: inline;">
              <button type="submit">Edit</button>
            </form>
          </li>
        <% end %>
      <% else %>
        <li>No tasks added yet.</li>
      <% end %>
    </ul>
  </main>
</body>
</html>
